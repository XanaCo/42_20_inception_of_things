apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wil-service-ingress
  namespace: dev
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wil-service
            port:
              number: 8888
